public class Mechanics extends WithLegs {
    /**
     * @param name имя механизма
     * @param run сколько механизм может пробежать
     * @param jump сколько механизм может перепрыгнуть
     */
    public Mechanics(String name,int run, int jump) {
        super(name, run, jump);
    }
}

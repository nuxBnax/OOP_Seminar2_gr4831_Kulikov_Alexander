public class Animals extends WithLegs {
    /**
     * @param name имя животного
     * @param run сколько животное может пробежать
     * @param jump сколько животное может перепрыгнуть
     */
    public Animals(String name, int run, int jump) {
        super(name, run, jump);
    }
}